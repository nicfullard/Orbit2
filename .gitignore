## .NET
bin/
obj/
*.user
*.suo
.vs/
*.swp
TestResults/

## Local secrets - never commit real credentials
appsettings.Development.json
appsettings.*.local.json
*.env
!deploy/orbit.env.example
# An Orbit Agent's registration (its credential). Normally lands in bin/, but never commit one from anywhere.
agent.json

## OS
.DS_Store
Thumbs.db

# Uploaded attachments (Attachments:Path); the deployed path is outside the app directory
App_Data/
